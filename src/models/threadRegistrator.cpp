#include "models/threadRegistrator.h"

// uint8_t ThreadRegistrator::start(Thread_t& thread, threadFunc func)
// {
//     auto r = thread.start(mbed::callback(func));
//     return r == osStatus_t::osOK;
// }